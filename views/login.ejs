<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Grocery Ledger</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@400;500;600&display=swap" rel="stylesheet">
    
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    
    <script type="module">
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#4E73DF',
              secondary: '#1CC88A', // Green
              accent: '#36B9CC',
              error: '#E74A3B',   // Red
              'bg-dark': '#0B0B0F', // Very dark background
              'card-dark': '#1A1A1C', // Dark card
            },
            fontFamily: {
              sans: ['"EB Garamond"', 'serif'],
            },
          }
        }
      }
    </script>
</head>

<body style="background-color: #0B0B0F;" class="bg-bg-dark text-gray-300 font-sans grid place-items-center min-h-screen p-4">

    <main class="w-full max-w-md">
        <form 
          action="/api/users/login" 
          method="POST" 
          class="bg-card-dark p-8 rounded-2xl shadow-xl border border-gray-700/50"
        >
            <h1 class="text-4xl text-secondary mb-6 text-center font-semibold">
                Welcome Back
            </h1>
            
            <% if (typeof error != 'undefined' && error) { %>
                <div class="bg-error/20 border border-error text-error text-center px-4 py-3 rounded-lg mb-4">
                    <%= error %>
                </div>
            <% } %>

            <div class="mb-4">
                <label class="block text-gray-300 text-lg mb-2" for="email">Email</label>
                <input 
                  type="email" 
                  id="email" 
                  name="email"
                  required
                  class="w-full px-4 py-3 bg-gray-800 text-gray-200 rounded-lg border border-gray-700 focus:outline-none focus:border-secondary focus:ring-1 focus:ring-secondary transition-colors"
                />
            </div>

            <div class="mb-6">
                <label class="block text-gray-300 text-lg mb-2" for="password">Password</label>
                <input 
                  type="password" 
                  id="password" 
                  name="password"
                  required
                  class="w-full px-4 py-3 bg-gray-800 text-gray-200 rounded-lg border border-gray-700 focus:outline-none focus:border-secondary focus:ring-1 focus:ring-secondary transition-colors"
                />
            </div>

            <button 
              type="submit" 
              class="w-full bg-secondary text-white text-xl py-3 rounded-lg font-semibold hover:bg-green-700 transition-all transform hover:scale-[1.02]"
            >
                Login
            </button>
            
            <p class="text-center text-gray-400 mt-6">
                Don't have an account? 
                <a href="/register" class="text-accent hover:underline">Sign Up</a>
            </p>
        </form>
    </main>

</body>
</html>