<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= typeof title != 'undefined' ? title : 'Grocery Ledger' %></title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    
    <script type="module">
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            fontFamily: {
              sans: ['"Outfit"', 'sans-serif'],
            },
            colors: {
              primary: '#6366f1',
              secondary: '#10b981',
              dark: '#050505',
              card: '#0a0a0a',
              border: '#1f1f1f'
            }
          }
        }
      }
    </script>
    <style>
        /* Force black background immediately */
        html, body { background-color: #050505 !important; color: #d1d5db; }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #050505; }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 10px; }
        
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

        /* Mobile Menu Animation */
        #mobile-menu {
            transition: max-height 0.3s ease-in-out, opacity 0.3s ease-in-out;
            max-height: 0;
            opacity: 0;
            overflow: hidden;
            background-color: #050505; /* SOLID BLACK - Fixes the see-through issue */
        }
        #mobile-menu.open {
            max-height: 500px;
            opacity: 1;
            border-bottom: 1px solid #1f1f1f;
        }
    </style>
</head>
<body class="bg-dark text-gray-300 font-sans min-h-screen flex flex-col antialiased selection:bg-primary selection:text-white">

    <header class="fixed top-0 w-full z-50 bg-dark border-b border-border">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                
                <a href="/dashboard" class="flex items-center gap-2 group flex-shrink-0 z-50">
                    <div class="w-8 h-8 bg-gradient-to-br from-primary to-purple-600 rounded-lg flex items-center justify-center shadow-lg shadow-primary/20 group-hover:scale-105 transition-transform">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5 text-white">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 1 0-7.5 0v4.5m11.356-1.993 1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 0 1-1.12-1.243l1.264-12A1.125 1.125 0 0 1 5.513 7.5h12.974c.576 0 1.059.435 1.119 1.007ZM8.625 10.5a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Zm7.5 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z" />
                        </svg>
                    </div>
                    <span class="text-lg font-bold text-white tracking-tight">Grocery<span class="text-primary">Calc</span></span>
                </a>
                
                <% if (typeof user != 'undefined' && user) { %>
                    
                    <nav class="hidden md:flex items-center gap-1">
                        <a href="/dashboard" class="px-3 py-2 rounded-lg hover:bg-white/5 text-gray-400 hover:text-white transition-colors text-sm font-medium">Inventory</a>
                        <a href="/calculator" class="px-3 py-2 rounded-lg hover:bg-white/5 text-gray-400 hover:text-white transition-colors text-sm font-medium">Calculator</a>
                        <a href="/history" class="px-3 py-2 rounded-lg hover:bg-white/5 text-gray-400 hover:text-white transition-colors text-sm font-medium">History</a>
                        <a href="/debts" class="px-3 py-2 rounded-lg hover:bg-white/5 text-gray-400 hover:text-white transition-colors text-sm font-medium">Ledger</a>
                        <a href="/settings" class="px-3 py-2 rounded-lg hover:bg-white/5 text-gray-400 hover:text-white transition-colors text-sm font-medium">Settings</a>
                        <a href="/logout" class="ml-4 px-4 py-2 bg-red-500/10 text-red-500 hover:bg-red-500 hover:text-white rounded-lg text-sm font-bold transition-colors">Logout</a>
                    </nav>

                    <button id="mobile-menu-btn" class="md:hidden p-2 text-gray-300 hover:text-white focus:outline-none z-50">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-7 h-7">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                        </svg>
                    </button>

                <% } %>
            </div>
        </div>

        <% if (typeof user != 'undefined' && user) { %>
        <div id="mobile-menu" class="w-full absolute top-16 left-0 md:hidden shadow-2xl z-50">
            <div class="p-4 space-y-3 flex flex-col bg-dark">
                <a href="/dashboard" class="block px-4 py-3 rounded-xl bg-gray-900 text-white font-medium border border-border hover:border-primary/50 hover:text-primary transition-all flex items-center gap-3">
                    Items
                </a>
                <a href="/calculator" class="block px-4 py-3 rounded-xl bg-gray-900 text-white font-medium border border-border hover:border-primary/50 hover:text-primary transition-all flex items-center gap-3">
                    Calculator
                </a>
                <a href="/history" class="block px-4 py-3 rounded-xl bg-gray-900 text-white font-medium border border-border hover:border-primary/50 hover:text-primary transition-all flex items-center gap-3">
                    Sales History
                </a>
                <a href="/debts" class="block px-4 py-3 rounded-xl bg-gray-900 text-white font-medium border border-border hover:border-primary/50 hover:text-primary transition-all flex items-center gap-3">
                    Debt Ledger
                </a>
                <a href="/settings" class="block px-4 py-3 rounded-xl bg-gray-900 text-white font-medium border border-border hover:border-primary/50 hover:text-primary transition-all flex items-center gap-3">
                    Settings
                </a>
                <div class="pt-2">
                    <a href="/logout" class="block px-4 py-3 rounded-xl bg-red-900/20 text-red-400 font-bold border border-red-900/30 text-center">
                        Logout
                    </a>
                </div>
            </div>
        </div>
        <% } %>
    </header>

    <div class="h-20"></div>

    <main class="flex-grow max-w-6xl mx-auto w-full p-4 pb-24">

    <script>
        const btn = document.getElementById('mobile-menu-btn');
        const menu = document.getElementById('mobile-menu');
        
        if(btn && menu) {
            btn.addEventListener('click', () => {
                menu.classList.toggle('open');
            });
        }
    </script>